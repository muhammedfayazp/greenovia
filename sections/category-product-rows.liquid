<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">

<section class="catprodrows">
  <div class="catprodrows__container page-width">

    {%- if section.blocks.size > 0 -%}
  <script src="{{ 'component-cart-quantity-selector.js' | asset_url }}" type="module" fetchpriority="low"></script>
{%- endif -%}

{% for block in section.blocks %}
  {% assign col = collections[block.settings.collection] %}
  {% if col != blank and col.products.size > 0 %}
    <div class="catprodrows__section" {{ block.shopify_attributes }}>
          
          <div class="catprodrows__head">
            <h2 class="catprodrows__title">{{ col.title }}</h2>
            <a href="{{ col.url }}" class="catprodrows__more">View all →</a>
          </div>

          <div class="catprodrows__swiper swiper swiper-{{ block.id }}">
            <div class="swiper-wrapper">
              {% for product in col.products limit: 12 %}
                <div class="catprodrows__card swiper-slide{% unless product.available %} catprodrows__card--soldout{% endunless %}">

                  <div class="catprodrows__img-container">
                    {% unless product.available %}
                      <span class="catprodrows__soldout-badge">Sold Out</span>
                    {% endunless %}

                    <a href="{{ product.url }}" class="catprodrows__img">
                      {% if product.featured_image %}
                        <img
                          src="{{ product.featured_image | image_url: width: 400 }}"
                          alt="{{ product.title | escape }}"
                          loading="lazy"
                        >
                      {% endif %}
                    </a>
                  </div>

                  <div class="catprodrows__info">
                    <div class="catprodrows__vendor">{{ product.vendor }}</div>
                    <a href="{{ product.url }}" class="catprodrows__name">{{ product.title }}</a>
                    
                    <div class="catprodrows__bottom">
                      <div class="catprodrows__price">
                        {%- if product.price == 0 -%}
                          {%- if product.compare_at_price > 0 -%}
                            <span class="dirham-symbol" aria-hidden="true">&#xE000;</span>{{ product.compare_at_price | money_without_currency }}
                          {%- else -%}
                            <span class="catprodrows__price--na">—</span>
                          {%- endif -%}
                        {%- else -%}
                          <span class="dirham-symbol" aria-hidden="true">&#xE000;</span>{{ product.price | money_without_currency }}
                        {%- endif -%}
                      </div>

                      {%- assign cart_qty = cart | item_count_for_variant: product.selected_or_first_available_variant.id -%}

                      <product-form-component class="catprodrows__form-wrapper" data-section-id="{{ section.id }}" data-product-id="{{ product.id }}" on:submit="/handleSubmit">
                        <div class="visually-hidden" aria-live="assertive" role="status" aria-atomic="true" ref="liveRegion"></div>
                        {%- form 'product', product, id: nil, class: 'catprodrows__form' -%}
                          <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" ref="variantId">
                          
                          <div class="catprodrows__actions {% if cart_qty > 0 %}is-active{% endif %}" data-cart-quantity="{{ cart_qty }}">
                          
                          <cart-quantity-selector-component class="catprodrows__qty-selector" data-variant-id="{{ product.selected_or_first_available_variant.id }}" ref="quantitySelector">
                            <button type="button" class="qty__btn" ref="minusButton" on:click="/decreaseQuantity">
                              <span class="qty__minus-icon">{{ 'icon-minus.svg' | inline_asset_content }}</span>
                              <span class="qty__trash-icon">{{ 'icon-delete.svg' | inline_asset_content }}</span>
                            </button>
                            <input 
                              type="number" 
                              name="quantity" 
                              value="{{ cart_qty | at_least: 1 }}" 
                              min="0" 
                              {% if product.selected_or_first_available_variant.quantity_rule.max %}max="{{ product.selected_or_first_available_variant.quantity_rule.max }}"{% endif %}
                              step="{{ product.selected_or_first_available_variant.quantity_rule.increment | default: 1 }}"
                              class="visually-hidden" 
                              ref="quantityInput"
                              data-cart-quantity="{{ cart_qty }}"
                              on:blur="/setQuantity"
                              on:focus="/selectInputValue"
                            >
                            <span class="qty__current" ref="quantityDisplay">{{ cart_qty }}</span>
                            <button type="button" class="qty__btn" ref="plusButton" on:click="/increaseQuantity">{{ 'icon-plus.svg' | inline_asset_content }}</button>
                          </cart-quantity-selector-component>

                          <add-to-cart-component class="catprodrows__atc-wrapper">
                            <button type="submit" class="catprodrows__atc-trigger" aria-label="Add to cart" ref="addToCartButton"
                              {% unless product.available %}disabled aria-disabled="true"{% endunless %}>
                              {{ 'icon-add-to-cart.svg' | inline_asset_content }}
                            </button>
                          </add-to-cart-component>
                        </div>
                      {%- endform -%}
                    </product-form-component>
                    </div>
                  </div>

                </div>
              {% endfor %}
            </div>
          </div>

        </div>
      {% endif %}
    {% endfor %}

  </div>
</section>

<style>
.catprodrows {
  padding: 60px 0;
  background: #FFFFFF;
}
.catprodrows__container {
  padding-left: 20px !important;
  padding-right: 20px !important;
  max-width: 1400px;
  margin: 0 auto;
}
.catprodrows__head {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  margin-bottom: 24px;
}
.catprodrows__title {
  margin: 0;
  font-size: 32px;
  color: #1f3d2b;
}
.catprodrows__more {
  color: #1f3d2b;
  text-decoration: none;
  font-weight: 500;
}
.catprodrows__card {
  display: flex;
  flex-direction: column;
  background: #fff;
  border-radius: 18px;
  overflow: hidden;
  box-shadow: 0 6px 18px rgba(0,0,0,.08);
  height: 100%;
}
/* Sold-out badge */
.catprodrows__soldout-badge {
  position: absolute;
  top: 10px;
  left: 10px;
  background: #888;
  color: #fff;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  padding: 4px 10px;
  border-radius: 999px;
  z-index: 2;
  pointer-events: none;
}

/* Dim sold-out cards slightly */
.catprodrows__card--soldout {
  opacity: 0.75;
}

/* Disable button style for sold-out */
.catprodrows__atc-trigger:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.catprodrows__img-container {
  position: relative;
  overflow: hidden;
  width: 100%;
  aspect-ratio: 1 / 1;
  background-color: #FFFFFF;
}

.catprodrows__img {
  display: block;
  width: 100%;
  height: 100%;
}

.catprodrows__img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.5s ease;
}

.catprodrows__card:hover .catprodrows__img img {
  transform: scale(1.05);
}

.catprodrows__info {
  padding: 12px; /* Reduced padding from 16px */
  display: flex;
  flex-direction: column;
  gap: 0; /* Removed gap completely */
  flex-grow: 1;
}

.catprodrows__vendor {
  font-size: 11px; /* Slightly smaller */
  text-transform: uppercase;
  color: #777;
  letter-spacing: 0.5px;
  margin-bottom: 2px;
}

.catprodrows__name {
  font-size: 15px; /* Reduced from 16px */
  font-weight: 600;
  color: #1f3d2b;
  text-decoration: none;
  line-height: 1.3; /* Tighter line height */
  height: 40px; /* Fixed height for 2 lines */
  margin-bottom: 0;
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.catprodrows__price {
  font-size: 16px; /* Reduced from 18px */
  font-weight: 700;
  color: #1f3d2b;
  white-space: nowrap;
}
.catprodrows__bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 4px; /* Minimal margin */
  gap: 8px;
}

/* Heart Icon */
.catprodrows__heart {
  position: absolute;
  top: 12px;
  right: 12px;
  width: 32px;
  height: 32px;
  background: #fff;
  border-radius: 50%;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  cursor: pointer;
  z-index: 2;
  transition: transform 0.2s ease;
  padding: 0;
  color: #1f3d2b;
}
.catprodrows__heart:hover { transform: scale(1.1); }
.catprodrows__heart svg { width: 18px; height: 18px; stroke-width: 1.5; }
.catprodrows__heart.is-active { color: #ff3b30; }

/* Actions & Buttons */
.catprodrows__actions {
  position: relative;
  display: flex;
  justify-content: flex-end;
  height: 34px;
  margin-top: 8px;
}

.catprodrows__atc-trigger {
  width: 34px;
  height: 34px;
  border-radius: 50%;
  background: #1f3d2b;
  color: #fff;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 1;
}

.catprodrows__atc-trigger svg {
  width: 17px;
  height: 17px;
}

.catprodrows__qty-selector {
  position: absolute;
  right: 0;
  top: 0;
  height: 34px;
  width: 34px;
  min-width: 34px;
  background: #1f3d2b;
  border-radius: 17px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 3px;
  overflow: hidden;
  opacity: 0;
  pointer-events: none;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-sizing: border-box;
  box-shadow: 0 4px 12px rgba(31, 61, 43, 0.2);
}

.catprodrows__actions.is-active .catprodrows__atc-wrapper {
  opacity: 0;
  pointer-events: none;
  transform: scale(0.8);
}

.catprodrows__actions.is-active .catprodrows__qty-selector {
  width: auto;
  min-width: 92px;
  opacity: 1;
  pointer-events: auto;
}

.catprodrows__qty-selector .qty__btn {
  width: 26px;
  height: 26px;
  flex-shrink: 0;
  border-radius: 50%;
  background: rgba(255,255,255,0.15);
  color: #fff;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  padding: 0;
  transition: background 0.2s ease;
}
.catprodrows__qty-selector .qty__btn:hover {
  background: rgba(255,255,255,0.25);
}

.catprodrows__qty-selector .qty__trash-icon {
  display: none;
}

.catprodrows__actions[data-cart-quantity="1"] .qty__minus-icon {
  display: none;
}

.catprodrows__actions[data-cart-quantity="1"] .qty__trash-icon {
  display: flex;
}

.catprodrows__qty-selector .qty__btn svg { 
  width: 14px; 
  height: 14px; 
  display: block;
}

.catprodrows__qty-selector .qty__current {
  color: #fff;
  font-weight: 700;
  font-size: 13px;
  line-height: 1;
  flex: 1;
  text-align: center;
  min-width: 20px;
}

.catprodrows__bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: auto;
  gap: 8px;
  min-height: 40px;
}

.catprodrows__price {
  font-size: 16px;
  font-weight: 700;
  color: #1f3d2b;
  white-space: nowrap;
}
.catprodrows__price--na {
  color: #aaa;
  font-weight: 400;
  font-size: 14px;
}

.catprodrows__form-wrapper {
  flex: 0 0 auto;
}

.catprodrows__atc-trigger:hover {
  background: #2a523a;
  transform: scale(1.05);
}

.catprodrows__atc-trigger:active {
  transform: scale(0.95);
}

.catprodrows__actions.is-loading .catprodrows__atc-trigger {
  pointer-events: none;
  opacity: 0.7;
}

.catprodrows__actions.is-loading .catprodrows__atc-trigger svg {
  animation: rotate 1s linear infinite;
}

@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Mobile */
@media (max-width: 768px){
  .catprodrows__container{
    padding-left: 16px !important;
    padding-right: 16px !important;
  }
  .catprodrows__card{
    max-width: 260px;
  }
  .catprodrows__title{ font-size:24px; }
  .catprodrows__section{
    margin-bottom: 40px;
  }
}
</style>

<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function () {
  // Initialize each category slider
  {% for block in section.blocks %}
    {% assign col = collections[block.settings.collection] %}
    {% if col != blank and col.products.size > 0 %}
      new Swiper('.swiper-{{ block.id }}', {
        slidesPerView: 1.2,
        spaceBetween: 16,
        grabCursor: true,
        freeMode: true,
        breakpoints: {
          480: { slidesPerView: 1.5 },
          640: { slidesPerView: 2.5 },
          990: { slidesPerView: 4, freeMode: false },
          1200: { slidesPerView: 5, freeMode: false }
        }
      });
    {% endif %}
  {% endfor %}

  // Bridge Logic for syncing theme components with custom UI
  const themeEvents = {
    cartUpdate: 'cart:update',
    qtyUpdate: 'quantity-selector:update'
  };

  // 1. Sync all cards when cart is updated globally
  document.addEventListener(themeEvents.cartUpdate, async (event) => {
    let cart = event.detail.resource;
    if (!cart || !cart.items) {
      const res = await fetch('/cart.js');
      cart = await res.json();
    }
    if (!cart || !cart.items) return;

    document.querySelectorAll('.catprodrows__form-wrapper').forEach(wrapper => {
      const variantId = wrapper.querySelector('[ref="variantId"]')?.value;
      if (!variantId) return;

      const item = cart.items.find(i => i.variant_id.toString() === variantId.toString());
      const qty = item ? item.quantity : 0;
      
      const actions = wrapper.querySelector('.catprodrows__actions');
      const display = wrapper.querySelector('.qty__current');
      const input = wrapper.querySelector('cart-quantity-selector-component input');

      if (actions) {
        actions.dataset.cartQuantity = qty;
        actions.classList.toggle('is-active', qty > 0);
      }
      if (display) display.textContent = qty;
      if (input) {
        input.dataset.cartQuantity = qty;
        input.value = qty > 0 ? qty : 1; // Keep input at 1 for next add if removed
      }
    });
  });

  // 2. Handle immediate updates from quantity selectors in this section
  document.addEventListener(themeEvents.qtyUpdate, async (event) => {
    const qtySelector = event.target.closest('cart-quantity-selector-component');
    if (!qtySelector || !qtySelector.closest('.catprodrows')) return;

    const wrapper = qtySelector.closest('.catprodrows__form-wrapper');
    const variantId = wrapper.querySelector('[ref="variantId"]')?.value;
    const quantity = event.detail.quantity;
    const display = qtySelector.querySelector('[ref="quantityDisplay"]');
    
    // Optimistic UI update for the display text
    if (display) display.textContent = quantity;

    try {
      // Trigger cart change
      const res = await fetch('/cart/change.js', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ 
          id: variantId.toString(), 
          quantity: quantity,
          sections_url: window.location.pathname
        })
      });
      const cart = await res.json();

      // Dispatch cart:update with forced cart-icon refresh
      const updateEvent = new CustomEvent(themeEvents.cartUpdate, {
        bubbles: true,
        detail: {
          resource: cart,
          data: {
            itemCount: cart.item_count,
            source: 'catprodrows'
          }
        }
      });
      document.dispatchEvent(updateEvent);

      // Sync all product cards on the page
      document.querySelectorAll('.catprodrows__actions').forEach(actions => {
        const vIdInput = actions.closest('product-form-component')?.querySelector('[name="id"]');
        const vId = vIdInput?.value;
        if (!vId) return;
        
        const item = cart.items.find(i => i.variant_id.toString() === vId.toString());
        const qty = item ? item.quantity : 0;
        
        actions.dataset.cartQuantity = qty;
        const display = actions.querySelector('.qty__current');
        if (display) display.textContent = qty;
        
        if (qty > 0) {
          actions.classList.add('is-active');
        } else {
          actions.classList.remove('is-active');
        }
      });
    } catch (e) {
      console.error('Cart update failed', e);
    }
  });

  // Handle successful ATCs from product-form.js too
  document.addEventListener('cart:update', async (event) => {
    if (event.detail?.data?.source === 'catprodrows') return;
    
    // If update came from elsewhere, sync our cards
    // Use the resource if available, otherwise fetch
    let cart = event.detail?.resource;
    if (!cart || !cart.items) {
      const res = await fetch('/cart.js');
      cart = await res.json();
    }
    
    document.querySelectorAll('.catprodrows__actions').forEach(actions => {
      const vIdInput = actions.closest('product-form-component')?.querySelector('[name="id"]');
      const vId = vIdInput?.value;
      if (!vId) return;
      const item = cart.items.find(i => i.variant_id.toString() === vId.toString());
      const qty = item ? item.quantity : 0;
      actions.dataset.cartQuantity = qty;
      const display = actions.querySelector('.qty__current');
      if (display) display.textContent = qty;
      if (qty > 0) actions.classList.add('is-active');
      else actions.classList.remove('is-active');
    });
  });

  // Wishlist functionality
  window.toggleWishlist = function(button) {
    const productId = button.dataset.productId;
    const productHandle = button.dataset.productHandle;
    
    try {
      let wishlist = JSON.parse(localStorage.getItem('wishlist') || '[]');
      const index = wishlist.findIndex(item => item.id === productId);
      
      if (index > -1) {
        wishlist.splice(index, 1);
        button.classList.remove('is-active');
      } else {
        wishlist.push({
          id: productId,
          handle: productHandle,
          addedAt: new Date().toISOString()
        });
        button.classList.add('is-active');
      }
      
      // Toggle SVGs
      const outline = button.querySelector('.heart-outline');
      const filled = button.querySelector('.heart-filled');
      if (outline && filled) {
        if (button.classList.contains('is-active')) {
          outline.style.display = 'none';
          filled.style.display = 'block';
        } else {
          outline.style.display = 'block';
          filled.style.display = 'none';
        }
      }
      localStorage.setItem('wishlist', JSON.stringify(wishlist));
    } catch (e) {
      console.warn('Wishlist: localStorage is not accessible.', e);
      // Toggle SVG without persistence
      button.classList.toggle('is-active');
    }
  };

  // Load wishlist state
  try {
    const wishlist = JSON.parse(localStorage.getItem('wishlist') || '[]');
    wishlist.forEach(item => {
      const buttons = document.querySelectorAll(`.catprodrows__heart[data-product-id="${item.id}"]`);
      buttons.forEach(button => {
        button.classList.add('is-active');
        const outline = button.querySelector('.heart-outline');
        const filled = button.querySelector('.heart-filled');
      if (outline) outline.style.display = 'none';
      if (filled) filled.style.display = 'block';
    });
  });
  } catch (e) {
    console.warn('Wishlist: localStorage is not accessible for loading state.', e);
  }
});
</script>

{% schema %}
{
  "name": "Category Product Rows",
  "settings": [],
  "blocks": [
    {
      "type": "category",
      "name": "Category",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Select collection"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Category Product Rows",
      "blocks": [
        { "type": "category" },
        { "type": "category" },
        { "type": "category" }
      ]
    }
  ]
}
{% endschema %}